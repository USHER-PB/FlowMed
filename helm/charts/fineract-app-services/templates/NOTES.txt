Thank you for installing {{ .Chart.Name }}!

Your Fineract Application Services deployment is now running.

Deployed Services:
{{- if (index .Values "payment-gateway" "enabled") }}
  - Payment Gateway (port {{ (index .Values "payment-gateway" "service" "port") | default 8082 }})
{{- end }}
{{- if (index .Values "asset-service" "enabled") }}
  - Asset Service (port {{ (index .Values "asset-service" "service" "port") | default 8083 }})
{{- end }}
{{- if (index .Values "customer-self-service" "enabled") }}
  - Customer Self-Service (port {{ (index .Values "customer-self-service" "service" "port") | default 8080 }})
{{- end }}
{{- if (index .Values "sms-gateway" "enabled") }}
  - SMS Gateway (port {{ (index .Values "sms-gateway" "service" "port") | default 8080 }})
{{- end }}

Global Configuration:
  - Keycloak URL: {{ .Values.global.keycloakUrl }}
  - Fineract URL: {{ .Values.global.fineractUrl }}
  - Storage Endpoint: {{ .Values.global.storage.endpoint }}

To verify the deployment:

  # Check all pods are running
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}

  # Check services
  kubectl get services -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}

  # View logs for a specific service
  kubectl logs -l app.kubernetes.io/component=payment-gateway -n {{ .Release.Namespace }} -f

For more information, see the README.md file.
