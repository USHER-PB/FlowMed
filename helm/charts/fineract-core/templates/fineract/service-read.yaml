{{/*
=============================================================================
Fineract Read Instance Service
=============================================================================
Validates: Requirements 1.7
=============================================================================
*/}}
{{- if .Values.fineract.read.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "fineract-core.fullname" . }}-read
  labels:
    {{- include "fineract-core.read.labels" . | nindent 4 }}
  {{- with (include "fineract-core.annotations" .) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fineract-core.read.selectorLabels" . | nindent 4 }}
{{- end }}
