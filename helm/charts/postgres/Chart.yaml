apiVersion: v2
name: postgres
description: |
  PostgreSQL Helm chart for the OM Platform.
  Supports standalone mode for local development, CNPG mode for HA with CloudNativePG,
  and external mode for cloud databases (RDS, Cloud SQL).

type: application
version: 1.1.0
appVersion: "17.2"

maintainers:
  - name: Platform Team
    email: platform@example.com

keywords:
  - postgresql
  - database
  - sql
  - storage

home: https://github.com/skyengpro/om
sources:
  - https://github.com/skyengpro/om

annotations:
  category: Database
  licenses: Apache-2.0
