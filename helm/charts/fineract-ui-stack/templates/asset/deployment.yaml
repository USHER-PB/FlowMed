{{/*
 =============================================================================
 Asset Deployment Template
 =============================================================================
 Deployment for Asset UI application (NGINX serving static files)
 Requirements: 7.2, 7.5, 8.5, 12.6, 13.6
 =============================================================================
 */}}
{{- if .Values.asset.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fineract-ui-stack.fullname" . }}-asset
  labels:
    {{- include "fineract-ui-stack.appLabels" (dict "root" . "appName" "asset") | nindent 4 }}
spec:
  replicas: {{ .Values.asset.replicaCount | default .Values.ui.replicaCount }}
  selector:
    matchLabels:
      {{- include "fineract-ui-stack.appSelectorLabels" (dict "root" . "appName" "asset") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "fineract-ui-stack.appSelectorLabels" (dict "root" . "appName" "asset") | nindent 8 }}
    spec:
      containers:
        - name: asset
          image: "{{ .Values.asset.image.repository }}:{{ .Values.asset.image.tag | default .Values.ui.image.tag }}"
          imagePullPolicy: {{ .Values.asset.image.pullPolicy | default .Values.ui.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: {{ .Values.asset.resources.requests.cpu | default .Values.ui.resources.requests.cpu }}
              memory: {{ .Values.asset.resources.requests.memory | default .Values.ui.resources.requests.memory }}
            limits:
              cpu: {{ .Values.asset.resources.limits.cpu | default .Values.ui.resources.limits.cpu }}
              memory: {{ .Values.asset.resources.limits.memory | default .Values.ui.resources.limits.memory }}
          volumeMounts:
            - name: config
              mountPath: /usr/share/nginx/html/assets/config.json
              subPath: config.json
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: {{ include "fineract-ui-stack.fullname" . }}-asset-config
{{- end }}
