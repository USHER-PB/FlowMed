{{/*
=============================================================================
Admin Ingress Template
=============================================================================
Ingress for Admin UI application with TLS and cert-manager
Requirements: 7.4, 9.1, 9.2, 9.3, 9.4, 9.5
=============================================================================
*/}}
{{- if and .Values.admin.enabled .Values.admin.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "fineract-ui-stack.fullname" . }}-admin
  labels:
    {{- include "fineract-ui-stack.appLabels" (dict "root" . "appName" "admin") | nindent 4 }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.admin.ingress.clusterIssuer | default "letsencrypt-prod" }}
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - {{ .Values.admin.hostname }}
      secretName: {{ .Values.admin.tls.secretName }}
  rules:
    - host: {{ .Values.admin.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "fineract-ui-stack.fullname" . }}-admin
                port:
                  number: 80
{{- end }}
