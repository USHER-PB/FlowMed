{{/*
=============================================================================
Branch Service Template
=============================================================================
ClusterIP service for Branch UI application
Requirements: 7.3
=============================================================================
*/}}
{{- if .Values.branch.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "fineract-ui-stack.fullname" . }}-branch
  labels:
    {{- include "fineract-ui-stack.appLabels" (dict "root" . "appName" "branch") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fineract-ui-stack.appSelectorLabels" (dict "root" . "appName" "branch") | nindent 4 }}
{{- end }}
