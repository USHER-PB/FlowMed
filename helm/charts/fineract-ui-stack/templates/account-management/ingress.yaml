{{/*
=============================================================================
Account Management Ingress Template
=============================================================================
Ingress for Account Management UI application with TLS and cert-manager
Requirements: 7.4, 9.1, 9.2, 9.3, 9.4, 9.5
=============================================================================
*/}}
{{- if and .Values.accountManagement.enabled .Values.accountManagement.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "fineract-ui-stack.fullname" . }}-account-management
  labels:
    {{- include "fineract-ui-stack.appLabels" (dict "root" . "appName" "account-management") | nindent 4 }}
  annotations:
    {{- if and .Values.accountManagement.tls.enabled .Values.accountManagement.ingress.clusterIssuer }}
    cert-manager.io/cluster-issuer: {{ .Values.accountManagement.ingress.clusterIssuer }}
    {{- end }}
    {{- if .Values.accountManagement.ingress.annotations }}
    {{- toYaml .Values.accountManagement.ingress.annotations | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: nginx
  {{- if .Values.accountManagement.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.accountManagement.hostname }}
      secretName: {{ .Values.accountManagement.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.accountManagement.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "fineract-ui-stack.fullname" . }}-account-management
                port:
                  number: 80
{{- end }}
